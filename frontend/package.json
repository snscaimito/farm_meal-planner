{
  "name": "mealplanner",
  "version": "1.0.0",
  "description": "Frontend for Meal Planner",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "prepare": "mkdir -p dist/js",
    "copyfiles": "copyfiles -u 1 src/* dist",
    "build": "npm run prepare & npm run copyfiles"
  },
  "author": "Stephan Schwab",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^16.1.0",
    "copyfiles": "^1.2.0",
    "http-server": "^0.11.1",
    "watchify": "^3.11.0"
  },
  "dependencies": {
    "jquery": "^3.3.1"
  }
}
